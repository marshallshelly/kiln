Third-party bundles, vendored so the examples run with no install step.

- `preact.umd.js`, `hooks.umd.js` — Preact 10, MIT licensed.
  https://github.com/preactjs/preact

Neither file is modified. They exist to demonstrate that an unmodified
framework build runs against Kiln's DOM.
